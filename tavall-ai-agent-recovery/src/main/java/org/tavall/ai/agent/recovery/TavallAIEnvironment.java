package org.tavall.ai.agent.recovery;

/** Trust environment of the observed logical node. AI execution is never authorized by this value alone. */
public enum TavallAIEnvironment {
    DEVELOPMENT,
    STAGING,
    PRODUCTION
}

package org.tavall.ai.builder;

/** External Builder artifact/evidence contracts consumed by Tavall AI domain composition. */
public enum TavallAIBuilderArtifactKind {
    BUILD_SPEC,
    SPONGE_SCHEMATIC,
    REPLAY,
    VISUAL_EVIDENCE,
    WORLD_BAKE_MANIFEST
}

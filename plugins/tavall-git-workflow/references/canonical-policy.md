# Canonical Git Policy Pointer

The source of truth is intentionally not copied into this skill.

Current shared policy location:

```text
repository: TavallStudios/tavall-project-novus
path: docs/quality/GIT_WORKFLOW.md
```

Always read the current version before consequential Git/PR workflow decisions when the repository is accessible.

Repository-specific `AGENTS.md`, `AGENT.MD`, `CONTRIBUTING.md`, synchronization contracts, staging manifests, release procedures, and deployment runbooks may impose stricter constraints. Stricter rules take precedence where the canonical policy allows repository-specific strengthening.

The skill should keep only stable operational invariants locally and defer evolving policy text to the canonical document. This prevents the skill and policy document from becoming divergent sources of truth.

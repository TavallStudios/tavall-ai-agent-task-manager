interface:
  display_name: "Tavall Skill Orchestrator"
  short_description: "Routes Tavall work through the right skills"
  default_prompt: "Use $tavall-skill-orchestrator as the entry point for Tavall-related work. Discover the current skill graph, route through Tavall memory when available, require $tavall-git-workflow for repository and PR policy, delegate to the smallest relevant domain skills, and surface degraded or missing dependencies instead of silently skipping them."

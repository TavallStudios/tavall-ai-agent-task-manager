package org.tavall.agent.builder;

/** Existing Builder artifact/evidence contracts consumed by the Builder agent. */
public enum TavallBuilderArtifactKind {
    BUILD_SPEC,
    SPONGE_SCHEMATIC,
    REPLAY,
    VISUAL_EVIDENCE,
    WORLD_BAKE_MANIFEST
}

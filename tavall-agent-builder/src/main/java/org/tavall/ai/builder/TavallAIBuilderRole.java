package org.tavall.ai.builder;

/** Builder domain behaviors composed inside a Tavall AI execution. */
public enum TavallAIBuilderRole {
    PLANNER,
    TERRAIN,
    ARCHITECTURE,
    DETAIL,
    REPAIR,
    VISUAL_CRITIC
}

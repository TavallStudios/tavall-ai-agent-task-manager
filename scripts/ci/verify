#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"
cd "$ROOT"

MAX_WORKERS="${TAVALL_CI_MAX_WORKERS:-2}"

exec ./gradlew \
  --no-daemon \
  --max-workers="$MAX_WORKERS" \
  clean \
  check
